# InfraCanvas frontend defaults.
# In serve-mode the dashboard talks to the same origin it was loaded from,
# so no WS/API URL is needed. This file is kept for `next dev` (frontend
# alone, pointed at a separately-running relay).
NEXT_PUBLIC_WS_URL=ws://localhost:7777
NEXT_PUBLIC_API_URL=http://localhost:7777
